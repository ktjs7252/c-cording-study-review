1.단어들을 저장하는 연결리스트 만들기
  -node 안에 data를 배열처리하여 해결완료
  
   /*typedef struct _node
{
    char name[30]; // 데이터 담을 공간
    struct _node* next; // 연결 도구!
} Node;*/


2.두개의 리스트를 한개로 합쳐보기
 -한개리스트의 tail에 그 다음 리스트의 head를 연결


3.리스트 역순으로 만들어보기
-head랑 tail 순서 바꿔서 연결

4. 원형 리스트
  -tail에 head를 연결시켜 끝나지 않는 리스트를 만듬
